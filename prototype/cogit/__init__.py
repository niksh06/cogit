"""Cogit reference prototype (ADR-0010): version control for agent cognition."""

# Must match the CHANGELOG.md top heading and both Cargo.toml versions
# (tools/interop-test.sh enforces the parity).
__version__ = "0.2.0"
